mixin like(value, isLiked)
  - let activeClass = "like_actual";

  button.like.js-like-button(
    class=isLiked ? activeClass : "",
    type="button",
    aria-label="Понравилось"
  )
    span.like__value.js-like__value= value
